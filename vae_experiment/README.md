# VocalArticulationEnvelope — Experimental Specification v1.1, steps 1–8

Offline implementation of `VAE_Experiment_Spec_v1_1.md`, which is the controlling
document. Where this README and the spec disagree, the spec wins.

**Authorized scope: steps 1–8 only.** Step 9 (reversal-existence gate) is not
crossed. Step 10 (statistical lock) is outstanding — **no statistical analysis
code exists in this repository**, deliberately: §13 is provisional until step 10
selects the estimation form and outcome coding, and freezing it early is a §14
Void condition. No human data is collected or prepared.

## Layout

```
vae/            the six stages, pure functions matching §21
  canonical.py    §3 canonical audio, §2 admission (format/duration/level)
  resampler.py    versioned polyphase resampler
  hear.py         §4 HEAR, §6 anchors, §2 admission (tempo/drift/pulse)
  slots.py        §5 authored slot masks (F6)
  shape.py        §6 I_effective, §7 partition and diagnostic caps
  lexicon.py      §9 CMUdict, ARPAbet, Maximum Onset Principle
  tables.py       F4 / F5 strict loaders — hard error, never a default
  sound.py        §7 d(c), §8 tiers, §10 four subscores
  rank.py         §10 verdicts to ranked list, §15 tie discipline
  conditions.py   C_FLAT and C_SHUFFLED envelopes
  oracle.py       §12 oracle contract and the anchor-delta diagnostic
  pairs.py        §11 five checks and the discard gate
  determinism.py  §15 contract, F9 golden comparison
  pipeline.py     stage assembly and §16 logging
  version.py      §15 EngineVersion from its seven inputs
  config.py       §18 versioned parameters
  constants.py    §18 hard-coded structural constants
config/         config_v1.json — every §18 parameter, plus the sweep points
fixtures/       F1–F9 (see fixture status below)
tools/          fixture generators and the §23 step runners
tests/          117 tests, including the §24 checklist assertions
reports/        step reports, regenerated by tools/steps.py
```

## Running

Generated audio fixtures (F1, F3, and the F2 stand-in) are not committed — they
are deterministic functions of their generators. Regenerate them first; the
committed F9 goldens then verify the regenerated set, which checks the generators
as well as the pipeline.

```bash
pip install -r requirements.txt
python3 tools/bootstrap.py          # regenerate F1, F3, F2_SYNTH, F8 ground truth
python3 tools/steps.py all          # steps 1, 2, 3, 4+5, 6, 7, 8 -> reports/
python3 tools/steps.py 1 --write-goldens   # regenerate F9 after a code change
python3 -m pytest tests -q
python3 tools/exercise.py           # steps 6-8 against SYNTHETIC_TEST_ONLY tables
```

Fixture generators (`tools/make_f1.py`, `make_f3.py`, `make_f2_synth.py`,
`make_synthetic_oracle.py`) are deterministic: regenerating reproduces the same
bytes.

## Fixture status

| ID | Status | Note |
|---|---|---|
| F1 | **Present** | 10 synthetic click tracks, known BPM/phase |
| F2 | **BLOCKED** | Real recorded accompaniment. `fixtures/F2_clips/ACCEPTANCE.md` |
| F3 | **Present** | 5 out-of-spec clips, 5 distinct rejection codes |
| F4 | **BLOCKED** | Needs Klatt (1976) and Crystal & House (1988) — see below |
| F5 | **BLOCKED** | Needs a named standard phonotactics reference — see below |
| F6 | **Authored** | 8 masks, 4 asymmetric, 2 mirror pairs |
| F7 | **BLOCKED** | Gated on F4, F5, F2 and F8 |
| F8 | **BLOCKED** | Needs human annotation. `fixtures/F8_oracle/ANNOTATOR_INSTRUCTIONS.md` |
| F9 | **Present** | Golden snapshots for 30 clips × 8 masks |

Run `python3 tools/readiness.py` for the live status of all five and what each
one is missing.

## Populating the blocked fixtures

Every path below **refuses** rather than substituting. None of them can be
satisfied with generated data.

| Fixture | You supply | Then run |
|---|---|---|
| F4 | `fixtures/F4_phone_durations/intake_f4.csv`, filled from the named papers | `python3 tools/import_f4.py` |
| F5 | `fixtures/F5_onset_clusters/intake_f5.csv`, transcribed from a named reference | `python3 tools/import_f5.py` |
| F2 | WAV files in `fixtures/F2_clips/intake/` + `intake_f2.csv` | `python3 tools/import_f2.py` |
| F8 | two annotators fill worksheets by ear, third adjudicates | `make_f8_worksheets.py` → `adjudicate_f8.py` |

Behaviour worth knowing:

* **F4** writes `POPULATED` only at full 24-consonant coverage. A partial table
  is reported as a gap list and left `UNPOPULATED` — a half-filled fixture would
  turn §22 failure #10 into a surprise at scoring time instead of a refusal at
  load time. A blank row is a *reported gap*; a half-filled row is an *error*.
* **F5** requires singleton onsets as well as clusters, since Maximum Onset
  Principle syllabification consults the table at every boundary. It records the
  dialect of each entry and reports a mismatch against the General American
  lexicon rather than absorbing it.
* **F2** runs the full §2 admission check and **excludes** failing clips with a
  reason code. Content, meter and language are declarations the pipeline cannot
  verify and are stamped as such.
* **F8** worksheets deliberately show no detected onsets, beat grid or pipeline
  anchors — an annotator shown the answer is not an independent measurement.
  Disagreements over 20 ms block the clip until a third annotator resolves them;
  agreement within 20 ms is averaged and the spread is kept as `anchor_sigma_s`
  so real annotator uncertainty reaches `I_effective`.

Populating F4 or F5 changes `duration_table_hash` / `onset_table_hash` and
therefore `EngineVersion` on every record. That is the intended behaviour.

`F2_SYNTH_pipeline_exercise/` is **not F2**. It exists to exercise SHAPE, the six
conditions and RANK. Every runner that reads it stamps `uses_synthetic_fixtures`.

**F4 and F5 are not guessed.** The loaders raise `FixtureUnpopulatedError` on any
lookup, and a phone missing from a populated F4 raises `MissingPhoneError` —
§22 failure #10: a duration-table gap is a hard error, never a silent default.
`tests/data/F4_synthetic.json` and `F5_synthetic.json` are arbitrary placeholders
carrying `status: SYNTHETIC_TEST_ONLY`; the loader refuses them unless a caller
passes `allow_synthetic=True`, and anything computed from them is stamped.

## §24 handoff checklist — where each item is enforced

| Checklist item | Enforced by |
|---|---|
| Offline only, no network calls | `tests/test_offline_and_purity.py` |
| Six stages are pure functions matching §21 | `vae/pipeline.py`, `test_offline_and_purity.py` |
| `shape()` does not branch on provenance | `tests/test_no_provenance_branch.py` (static + behavioural) |
| `I_effective` used for all budgeting | `vae/shape.py`, `test_shape_and_partition.py` |
| No hysteresis, no state, no history | `test_shape_and_partition.py::test_tiers_have_no_history` |
| `feasibility_floor_s` not in `EnvelopeSequence` | `test_shape_and_partition.py` |
| `d_pre_max`/`d_post_max` never read by scoring | `tests/test_diagnostics_not_scored.py` (static + behavioural) |
| `s_fit` is the only articulatory term | `test_lexicon_and_sound.py` |
| All six conditions implemented | `test_conditions_rank_pairs.py`, `tools/exercise.py` |
| §11 checks discard failing pairs | `test_conditions_rank_pairs.py`, `vae/pairs.py` |
| Single versioned config, every §18 parameter | `tests/test_config_and_version.py` |
| `EngineVersion` from all seven hashed inputs | `test_config_and_version.py` |
| Ties lexicographic, no dict/set ordering | `test_conditions_rank_pairs.py` |
| No RNG, no wall-clock | `test_offline_and_purity.py` |
| Determinism regression against F9 | `tests/test_determinism_regression.py` |
| §2 rejection tested against F3 | `test_canonical_and_rejection.py` |
| Missing phone → hard error, OOV → ABSTAIN | `test_fixtures_refuse_to_guess.py`, `test_lexicon_and_sound.py` |
| No statistical analysis code frozen | nothing in `vae/` imports a stats library |
| §16 logging complete | `vae/pipeline.py` |
| Nothing on the §19 DO NOT BUILD list exists | `test_offline_and_purity.py` |

## Parameter sweeps (§18, §26)

The sweep is **reported, never used to select a favourable configuration**. Two
are exercised in steps 1–8; the rest (`F_MIN_BASE`, `gamma`, `I_reference`) feed
SOUND, which is blocked on F4/F5.

| Sweep | Where | Result |
|---|---|---|
| `SIGMA_COEF` ∈ {0, 1, 2} | `reports/step4_5.json` | Mean `I_effective` shrinkage 0.00 / 18.33 / 36.65 ms. `SIGMA_COEF = 0` reduces `I_effective` to the identity, so the sweep answers whether uncertainty modelling matters rather than assuming it does. |
| `W_match` ∈ {50, 70, 90} ms | `reports/step2.json`, `reports/step4_5.json` | **Completely inert on both fixture families.** Identical anchor error, identical `GRID_ONLY` counts, identical gate outcome at every point. |

**The inert `W_match` sweep is a limitation of the fixtures, not a property of
the parameter, and both reports say so.** F1 and the F2 stand-in are quantised:
every onset is either well inside 50 ms of a slot position or well outside
90 ms, so no supporting set ever changes. Real accompaniment carries human
microtiming that puts onsets at intermediate distances, which is exactly where
`W_match` bites. Read three identical rows as *"untested here"*, never as
*"insensitive"* — the reports carry `sweep_is_informative: false` and an
`inertness_caveat` so the distinction survives into the record.

This matters because it is the same shape as v1.0 defect 3 — a swept parameter
that answers nothing. Here the parameter *is* consumed; the fixtures just cannot
exercise it. Re-running this sweep is the first thing worth doing once real F2
clips exist.

## Implementation decisions

Places where the spec is terse and a choice had to be made. Each is
deterministic, carries no learned parameter, and is logged.

1. **Onset time refinement** (`hear.py`). A spectral-flux peak localises a
   transient only to its 2048-sample window, a bias of up to 46 ms that the
   5.8 ms F1 gate would not survive. The onset is refined to the 10 % point of
   the energy rise — the same 10 % point §4's attack-sharpness definition uses —
   searched inside the interval the flux definition pins the transient to, and
   found by backtracking from the local peak so a modulated baseline cannot pin
   it to the window start.
2. **Tempo** (`hear.py`). ODF autocorrelation read as a harmonic comb over a fine
   period grid inside 70–140 BPM. Still a single global estimate from the ODF
   autocorrelation; the comb reads the same ACF at `h·T` so period resolution is
   not capped by the 5.805 ms frame lag.
3. **Beat phase** (`hear.py`). §4's objective exactly. It is piecewise-constant in
   phase, so ties within `epsilon_num` break on the smaller salience-weighted
   squared residual, then the smaller phase.
4. **Audible pulse** (`hear.py`). §2's "audible percussive or clearly articulated
   pulse" is operationalised as a pulse being both *present* (grid-match rate)
   and *articulated* (onset density no greater than the eighth lattice can hold).
   Grid-match rate alone is satisfied by dense texture.
5. **`LEAD_IN` and `PHRASE_TAIL` uncertainty** (`shape.py`). Neither has a second
   measured endpoint, so the shrinkage term uses the one real anchor's sigma with
   the configured endpoint contributing zero.
6. **`C_FLAT` constants** (`conditions.py`). The fixed uniform grid uses
   `I_reference` and `SIGMA_GRID_ONLY` from the versioned config — already-fixed
   constants, and not clip-derived, which is the control's whole point.
7. **`C_SHUFFLED` donor** (`conditions.py`). The lexicographically next `audio_id`
   among slot-count-matched clips, wrapping. Deterministic, no RNG.
8. **Cross-word resyllabification is not performed** (`lexicon.py`). It is not
   specified, and it would make a line's syllable boundaries depend on its
   neighbours, which §11's position-for-position matching cannot tolerate.
9. **R1 whole-beat guard tolerance** (`oracle.py`). A real one-beat phase slip
   lands *near* the beat period, never on it, so the guard compares with one
   analysis hop of slack. Slack in this direction is the safe one: R1 excludes
   rather than corrects.

## Not built

Nothing on the §19 DO NOT BUILD list. In particular: no downbeat identification,
no pitch/f0, no chord transcription, no source separation, no neural G2P, no
neural MIR, no LM generation, no persistence beyond flat files, and — per the
step-10 gate — no statistical analysis code.
